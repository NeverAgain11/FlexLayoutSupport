import XCTest
@testable import FlexBoxSupport

final class FlexBoxSupportTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FlexBoxSupport().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
